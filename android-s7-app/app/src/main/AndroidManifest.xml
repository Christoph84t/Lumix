<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lumix.s7plc">

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="28" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".S7PlcApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_plc"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LumixS7PLC">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MonitorActivity"
            android:exported="false"
            android:label="@string/title_monitor"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.DataBlockActivity"
            android:exported="false"
            android:label="@string/title_datablock"
            android:parentActivityName=".ui.MainActivity" />

    </application>

</manifest>
